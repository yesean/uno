{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend of uno",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../frontend && npm run build --prod && cp -r build ../backend/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "socket.io": "^2.4.0"
  }
}
